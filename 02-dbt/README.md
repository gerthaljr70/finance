# DBT – Data Transformation

This folder follows the standard DBT project structure:

- `models/` → Staging, intermediate, and marts
- `tests/` → Generic and custom tests
- `sources/` → Source definitions (raw tables)

DBT is the transformation engine of the project, following ELT principles.
